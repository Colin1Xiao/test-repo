# 重复进程/重叠职责排查清单

**生成时间**: 2026-03-19 18:18
**状态**: 待排查
**优先级**: 高

---

## 一、已明确的重复运行

### 1. 交易系统双版本并行

| 进程 | PID | 参数 | 启动方式 |
|------|-----|------|----------|
| 小龙交易 v4.1 | 45841 | - | launchd 守护 |
| V5.2 交易系统 | 74374 | `--live --execute --interval 15` | 手动/脚本 |

**风险等级**: 🔴 高

**待确认问题**:
- [ ] v4.1 和 v5.2 是否处理同一标的？
- [ ] v4.1 和 v5.2 是否使用同一 API Key？
- [ ] v5.2 是否本应影子运行 (`--shadow`) 但误用了 `--live --execute`？
- [ ] 两套系统是否可能对同一信号同时执行？

**排查命令**:
```bash
# 检查 v4.1 配置
cat ~/.openclaw/workspace/xiaolong_trading_system_4.1/config.json

# 检查 v5.2 配置
cat ~/.openclaw/workspace/xiaolong_trading_system_4.2/config.json

# 检查两套系统的标的
ps aux | grep xiaolong
ps aux | grep v52
```

**建议行动**:
1. 立即确认 v5.2 的运行模式是否符合预期
2. 如 v5.2 应为影子模式，立即切换或停止
3. 明确 v4.1 和 v5.2 的职责分工
4. 如无并行必要，停止旧版本

---

## 二、高度可疑的职责重叠

### 2. OCNMPS 与 Gateway

| 进程 | PID | 职责 | 状态 |
|------|-----|------|------|
| Gateway | 587 | 主服务入口 | 🟢 已确认 |
| start_ocnmps.sh | 585 | 桥接/辅助？ | 🟡 待确认 |

**风险等级**: 🟡 中

**待确认问题**:
- [ ] OCNMPS 是否有独立输入输出？
- [ ] OCNMPS 是否承担 Gateway 已覆盖的功能？
- [ ] OCNMPS 是桥接进程还是启动残留？
- [ ] 是否有对应的日志/端口/服务行为？

**排查命令**:
```bash
# 检查 OCNMPS 相关文件
ls -la ~/.openclaw/workspace/ocnmps/

# 检查是否有端口监听
lsof -i -P | grep 585

# 检查进程树
pstree -p 585
```

**建议行动**:
1. 确认 OCNMPS 实际功能
2. 如为启动残留，清理进程
3. 如为桥接进程，明确与 Gateway 的边界
4. 更新系统地图文档

---

## 三、结构性功能重叠

### 3. 健康检查/恢复脚本交叉

| 脚本 | 功能 | 版本 | 主用？ |
|------|------|------|--------|
| `openclaw-health-check.sh` | 健康检查 | 主版 | ❓ |
| `test-health-check.sh` | 测试用 | 测试版 | ❌ |
| `system-overview.sh` | 系统概览 | - | ❓ |
| `self-heal-advisor.sh` | 自愈建议 | v1 | ❓ |
| `recovery-controller.sh` | 恢复控制 | v2.5 | ❓ |
| `recovery-controller-v3.sh` | 恢复控制 | v3 | ❓ |

**风险等级**: 🟡 中

**待确认问题**:
- [ ] 哪个健康检查脚本是主链路？
- [ ] recovery-controller v2.5 和 v3 哪个在生产用？
- [ ] self-heal-advisor 是否已被 recovery-controller 取代？
- [ ] 多个脚本是否存在巡检逻辑重复？

**排查命令**:
```bash
# 检查 launchd 调用的是哪个脚本
cat ~/Library/LaunchAgents/com.colin.openclaw.autoheal.plist

# 检查各脚本最近运行时间
ls -lt ~/.openclaw/workspace/scripts/*.sh

# 检查日志调用记录
grep -r "recovery-controller" ~/.openclaw/workspace/logs/
```

**建议行动**:
1. 明确主用脚本，标记废弃脚本
2. 清理或归档测试/旧版本脚本
3. 更新 launchd 配置指向正确版本
4. 在文档中记录版本演进关系

---

## 四、排查优先级

| 优先级 | 问题 | 风险 | 判定标准 | 通过条件 | 建议截止 |
|--------|------|------|----------|----------|----------|
| P0 | 交易系统双版本并行 | 🔴 高 | 标的、账户、信号源、执行链路是否重叠 | 至少有明确隔离，或关闭其中一套 live 执行 | 立即 |
| P1 | OCNMPS 职责确认 | 🟡 中 | 是否存在独立输入输出与实际功能调用 | 明确归类为独立、附属或残留之一 | 24h |
| P2 | 恢复脚本版本清理 | 🟡 中 | 是否存在唯一主恢复链 | 主脚本唯一，旧版本标记或下线 | 本周 |

---

## 五、判断口径

> **当前存在重复功能运行的风险迹象，最明显的是交易系统双版本并行；此外，OCNMPS 与 Gateway、以及部分健康检查/恢复脚本之间可能存在职责重叠，需进一步核对主从关系与实际执行边界。**

---

## 六、排查结果记录

### 交易系统双版本 (已完成排查 + 处置)

**最终状态**: v4.1 已下线，v5.2 为唯一交易系统，主网实盘运行已确认预期。

| 项目 | 值 | 确认状态 |
|------|-----|----------|
| 运行模式 | MAINNET/LIVE | ✅ 预期行为 |
| 标的 | ETH/USDT:USDT | ✅ 预期 |
| 杠杆 | 100x | ✅ 已知 |
| 止损 | -0.5% | ✅ 已知 |
| 账户 | 待核对 | ⚠️ 需确认 API Key |

**v4.1 处置记录** (18:56):
```bash
kill 45841                    # 停止 v4.1 进程
launchctl bootout ...         # 禁用 launchd 任务
plist → .plist.disabled       # 防止自动重启
```

**管理层口径**:
> v4.1 已下线，v5.2 现为唯一交易执行系统，并按预期在主网实盘运行。当前重点已转为账户环境核对、交易边界控制、运行监视落地及配置优先级逻辑整改。

**遗留问题**:
- [ ] 核对 API Key 对应账户名称/UID
- [ ] 确认是否为预期实盘账户
- [ ] 确认交易边界参数生效
- [ ] 固化配置优先级（网络与执行分离）
- [ ] 建立运行监视机制（每日更新四项数据）

**判定结论**:
- ✅ **标的隔离**: BTC vs ETH，不会对同一标的重复下单
- ✅ **日志隔离**: 各自独立 logs/ 目录
- ✅ **执行状态隔离**: v4.1 IDLE，v5.2 已触发执行
- 🔴 **v5.2 配置-执行不一致**: 文件 `paper_trading: true` 被命令行 `--live --execute` 覆盖
- 🔴 **v5.2 账户环境**: 实际连接 **MAINNET (主网)**，非 Testnet！
- 🔴 **账户重叠风险**: v4.1 和 v5.2 都连接实盘

**日志证据**:
```
模式: LIVE (实盘)
网络: MAINNET (主网)
✅ 执行器初始化成功 (MAINNET)
⚡ LIVE MODE - calling executor...
```

**根本原因**:
```python
# run_v52_live.py 原代码
testnet = not args.live  # --live 使得 testnet=False，错误地连接主网
```

**代码修复** (已完成):
```python
# 修复后：网络选择与执行模式分离
# --testnet / --mainnet: 网络选择（互斥）
# --execute / --shadow: 执行模式（独立）
# --live: 废弃，显示警告
if args.mainnet:
    testnet = False  # 显式指定主网
else:
    testnet = True   # 默认测试网
```

**总判断**:
> 🔴 **CRITICAL**: v5.2 当前实际连接 MAINNET (主网)，而非配置文件预期的 Testnet。代码逻辑已修复，网络选择与执行模式已分离。

**风险级别**: 🔴 **CRITICAL** - 非预期实盘连接（代码已修复，待重启生效）

**管理层口径**:
> v5.2 配置与实际执行严重不一致：配置预期 Testnet，实际连接 MAINNET 主网。v4.1 也连接实盘但处于 IDLE 状态。代码逻辑已修复，网络选择与执行模式已分离。

**处置建议**:

| 时间 | 行动 | 状态 |
|------|------|------|
| **🔴 立即** | 停止 v5.2 进程 或 重启使用 `--testnet --shadow` | 待执行 |
| **🔴 立即** | 检查 v5.2 是否已执行任何实盘交易 | 待确认 |
| ✅ 已完成 | 修复代码逻辑：网络选择与执行模式分离 | 已提交 |
| 今天 | 若 v4.1 已废弃，停止残留进程 | 待确认 |
| 本周 | 固化配置优先级规则 | 待规划 |

---

### OCNMPS 职责 (待排查)

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 独立输入输出 | - | - |
| 与 Gateway 边界 | - | - |
| 功能确认 | - | - |
| 处置决策 | - | - |

### 恢复脚本主从 (待排查)

| 检查项 | 结果 | 备注 |
|--------|------|------|
| 主用健康检查脚本 | - | - |
| 主用恢复脚本 | - | - |
| 废弃脚本清单 | - | - |
| 处置决策 | - | - |

---

_文档状态: P0 已排查，P1/P2 待排查_
_责任人: Colin_
_下次检查: 2026-03-20_
_P0 排查时间: 2026-03-19 18:26_